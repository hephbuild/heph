# The go provider's codegen root: this widens the `go_src` / `go_embed_src` query
# scope from the target's own package to this whole subtree.
provider_state(
    provider = "go",
    go_codegen_root = True,
)
