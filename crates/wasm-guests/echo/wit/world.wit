package component:echo;

/// Minimal de-risk world: a single greet export to validate the
/// cargo-component guest + wasmtime host vertical slice.
world echo {
    export greet: func(name: string) -> string;
}
