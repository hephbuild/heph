[package]
name = "selfupdate"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
hconfig = { package = "config", path = "../config" }
hcore = { package = "core", path = "../core" }
anyhow = "1.0.102"
thiserror = "2.0.18"
tracing = "0.1"
# Downloads the pinned release binary.
hfetch = { package = "fetch", path = "../fetch" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3.27"
