[package]
name = "testkit"
version = "0.1.0"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
heph = { path = "../.." }
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }
anyhow = "1"
tempfile = "3"
