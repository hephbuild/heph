edition = "2023";
package heph.hproto.v1;
import "google/protobuf/go_features.proto";
option features.(pb.go).api_level = API_HYBRID;

message Sample {
  message Nested {
    repeated string included = 1;
    repeated string excluded = 2;
  }

  repeated string included = 1;
  repeated string excluded = 2;
  Nested nested = 3;
  repeated Nested repeated_nested = 4;
}
